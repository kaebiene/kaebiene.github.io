function setup() {
  createCanvas(windowWidth,windowHeight);
}

function draw(){
    textFont('Exo');
    textSize(200);
    text('HELLO',100,100);
    //console.log(drawingContext);
    //drawingContext.font = 'normal 200px Exo2'
}
